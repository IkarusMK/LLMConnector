# EXACT pin — the auth stack must never change as a side effect of an image
# rebuild. fastmcp ≥3.4 forwards the RFC 8707 `resource` parameter to the
# upstream IdP and adds a /consent interstitial; server.py neutralizes both
# (forward_resource=False, require_authorization_consent="external") and
# tests/test_oauth_upstream.py pins that contract, so version bumps arrive
# ONLY via Dependabot PRs that pass those guards.
fastmcp==3.4.6
paho-mqtt>=2.1,<3
# SSH / SFTP for ssh_tools (remote commands + file transfer)
paramiko>=5.0.0,<6
# Secure XML parsing for the CalDAV/WebDAV server responses (blocks entity-expansion
# / billion-laughs DoS from a malicious or compromised registered server).
defusedxml>=0.7.1,<1
# Keep the explicit DiskStore client_storage working on FastMCP 3 (v3's default
# moved to FileTreeStore due to a diskcache CVE; the disk extra is the
# sanctioned way to keep DiskStore — ours is Fernet-encrypted, self-data only).
# NOTE: bound this once a stable upper major is known; Dependabot + the CI pip-audit
# step (see .github/) track advisories on all deps in the meantime.
py-key-value-aio[disk]>=0.4.5
# Native (libuv) event loop. uvicorn — which FastMCP's HTTP transport runs on — picks it
# up automatically, and server.py installs it explicitly at startup. Faster async I/O for
# the connector's real bottleneck (network / event loop). Linux & macOS only (uvloop has
# no Windows wheels), so it's gated by a marker; the startup import is guarded too.
uvloop>=0.19; platform_system != "Windows"
# Image handling for fs_view / seeing workspace files with vision (scans, e-mail
# attachments, downloads) — Pillow downscales/normalizes raster images, pypdfium2
# rasterizes PDF pages (Apache/BSD, bundles PDFium, no system libs like poppler).
# cp314 linux wheels verified via `uv pip compile --only-binary :all:` before adding.
pillow>=11,<13
pypdfium2>=4,<6
